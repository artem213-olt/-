// Стек
import java.util.Stack; Stack<Integer> stack = new Stack<>(); stack.push(1); stack.pop();

// Список (связный)
import java.util.LinkedList; LinkedList<Integer> list = new LinkedList<>(); list.add(1);

// Массив (статический)
int[] arr = new int[5]; arr[0] = 1;

// Массив (динамический)
import java.util.ArrayList; ArrayList<Integer> arrayList = new ArrayList<>(); arrayList.add(1);