// Стек
#include <stack>; std::stack<int> s; s.push(1); s.pop();

// Список (двусвязный)
#include <list>; std::list<int> lst; lst.push_back(1);

// Массив (статический)
int arr[5]; arr[0] = 1;

// Массив (динамический)
#include <vector>; std::vector<int> vec; vec.push_back(1);